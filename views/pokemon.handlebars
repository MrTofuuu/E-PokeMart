{{#each pokemons as |pokemon| }}
<div class="item-card card" style="width: 18rem">
    <img class="card-img-top" src="{{{pokemon_sprites}}}" alt="Card image cap" />
    <div class="card-body">
        <h5 class="card-title">Pokemon:{{pokemon_name}}</h5>
        <p class="card-text">Type:{{pokemon_type}}</p>
        <p class="card-text">Price:{{price}}</p>
        <a href="" id="pokemon-button" class="btn btn-primary">
            Add to cart!
        </a>
    </div>
</div>
{{/each}}